language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  coverage run --include './*' --omit 'manage.py' -m manage test
# command to run after tests
after_success:
  coveralls
